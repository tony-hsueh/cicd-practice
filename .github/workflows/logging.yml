name: display all logging methods

on: workflow_dispatch

jobs:
  logging:
    name: logging
    runs-on: ubuntu-latest
    steps:
      - name: 展示 echo 語法
        run: |
          echo "This is a standard output message"
          echo "This is a standard error message" >&2
          echo "::warning::This is a warning message"
          echo "::error::This is an error message"
          echo "::debug::This is a debug message"
          echo "::notice::This is a notice message"

      - name: 展示 echo 分組效果
        run: |
          echo "::group::📦 第一個分組"
          echo "這裡是分組內容 1"
          echo "這裡是分組內容 2"
          echo "這裡是分組內容 3"
          echo "::endgroup::"

          echo "::group::🧪 第二個分組"
          echo "這是另一個分組"
          echo "可以看到內容被摺疊起來"
          echo "::endgroup::"

          echo "::group::🏗️ 第三個分組"
          echo "分組可以讓長輸出更整潔"
          echo "點擊可以展開/收合"
          echo "::endgroup::"

      - name: 展示 core 語法
        uses: actions/github-script@v7
        with:
          script: |
            core.info("This is a core.info message");
            core.warning("This is a warning");
            core.error("This is an error");
            core.notice("Notice 訊息");

      - name: 展示 core 分組效果
        uses: actions/github-script@v7
        with:
          script: |
            core.startGroup("第一個分組");
            console.log('a')
            console.log('b')
            console.log('c')
            core.endGroup();

            core.startGroup("第二個分組");
            core.error('這是錯誤訊息')
            core.endGroup();

            core.startGroup("第三個分組");
            console.log('d')
            core.endGroup();

      - name: 展示 console 語法
        uses: actions/github-script@v7
        with:
          script: |
            console.log("This is a console log message")
            console.warn("This is a console warning message")
            console.error("This is a console error message")
