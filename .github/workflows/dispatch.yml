name: test

on: workflow_dispatch

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Show workflow version
        run: |
          echo "Workflow version: 1.0-test_a_no_checkout"

      - name: Verify code content
        run: |
          echo "================================================"
          echo "📍 Proof: Branch Identifier"
          echo "================================================"
          cat branch-identifier.txt
          echo ""

          echo "================================================"
          echo "📍 GitHub Context (for reference)"
          echo "================================================"
          echo "github.ref: ${{ github.ref }}"
          echo "github.sha: ${{ github.sha }}"
